# shellscript

## cghosts
hostsファイルをコマンドで書き換える (Mac)
```
Usage: cghosts [options] <ipAddr> <hostname>

  -v        Print the version and exit.
  -h        Print this help.

Options:
  -i        Information.
  -l        Registered Hosts.
  -a        Add Hosts.
  -r        Remove Hosts.
```
